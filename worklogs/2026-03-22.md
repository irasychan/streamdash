# 2026-03-22 — Fix ESLint react-compiler errors

## Fix: React compiler lint errors (2 files)

**Priority:** Low (code quality)

Resolved both `react-compiler/react-compiler` errors flagged by ESLint — synchronous `setState` inside `useEffect` causing cascading re-renders.

### ChatContainer.tsx

- Removed the auto-deselect `useEffect` (cleared `selectedMessageId` when a message left the array)
- This effect was redundant: `selectedMessage` useMemo already returns `null` when the ID doesn't match, and all handlers/renderers guard against that

### WidgetConfigCard.tsx

- Replaced `useState("")` + `useEffect(() => setOrigin(window.location.origin))` with `useSyncExternalStore`
- `useSyncExternalStore` is React's recommended hook for reading browser-only values with SSR-safe fallback

**Files modified:**

- `features/chat/components/ChatContainer.tsx`
- `features/widgets/components/WidgetConfigCard.tsx`

## Session Complete
